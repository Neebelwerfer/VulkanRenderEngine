set(TEXTURE_FILES
    Texture2D.cpp
    Texture2D.h
    TextureCubemap.cpp
    TextureCubemap.h
)

list(TRANSFORM TEXTURE_FILES PREPEND "${CMAKE_CURRENT_SOURCE_DIR}/")
set(GRAPHICS_FILES ${GRAPHICS_FILES} ${TEXTURE_FILES} PARENT_SCOPE)